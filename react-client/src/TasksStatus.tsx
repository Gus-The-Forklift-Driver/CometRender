import React from "react";
import { ReactNode } from "react";

export class TasksStatus extends React.Component<{ token: string }> {
  constructor(p: any) {
    super(p);
  }
  render(): ReactNode {
    return <>tasks status</>;
  }
}
